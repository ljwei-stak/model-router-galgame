# GAL视图对话框不显示问题排查

## 问题现象
- GAL视图界面正常显示（背景+立绘）
- 底部输入框可见
- 但发送消息后没有对话框显示

## 可能原因

### 1. 对话框元素被隐藏或位置错误

**检查方法**：
```javascript
// 按 F12 打开控制台，运行：
document.querySelectorAll('[class*="dialogue"]')
document.querySelectorAll('[class*="text-box"]')
```

### 2. CSS 样式问题

对话框可能被隐藏在屏幕外或透明度为0。

### 3. 场景配置问题

`gal-scene.json` 可能没有正确配置对话框元素。

---

## 立即诊断步骤

### 步骤1: 检查浏览器控制台

```bash
# 按 F12 打开开发者工具
# 切换到 Console 标签
# 查看是否有错误（红色）
```

**常见错误**：
- `Cannot find element`
- `dialogue-text is undefined`
- CSS 加载失败

### 步骤2: 检查场景配置

打开 `gal-scene.json`，确认包含 `dialogue-text` 元素：

```json
{
  "elements": [
    {
      "type": "dialogue-text",
      "id": "dialogue-1",
      "x": 100,
      "y": 500,
      "w": 800,
      "h": 200,
      "hidden": false
    }
  ]
}
```

### 步骤3: 检查消息是否实际发送

```bash
# 在控制台运行：
console.log('Messages:', document.querySelectorAll('[data-message]'))
```

---

## 快速修复尝试

### 方案1: 重置场景配置

```bash
cd F:\DeepSeek_harness\model-router-galgame

# 备份当前配置
copy gal-scene.json gal-scene.json.backup

# 检查配置文件
type gal-scene.json
```

### 方案2: 切换到编辑模式检查

在 GAL 视图界面：
1. 点击顶部 **"编辑模式"** 按钮
2. 查看场景中是否有 **对话文本框** 元素
3. 如果没有，需要添加

### 方案3: 检查是否在状态页

GAL视图可能停留在"状态页"（等待模型响应）：
- 状态页只显示状态文字，没有对话框
- 等待模型完成回复后应该会切换到对话显示

---

## 详细排查清单

请帮我确认以下信息：

### A. 控制台错误
- [ ] 按 F12，Console 标签有红色错误吗？
- [ ] 错误内容是什么？

### B. 消息是否发送
- [ ] 发送消息后，左侧"历史记录"有新对话吗？
- [ ] 输入框的消息是否被清空？

### C. 模型是否响应
- [ ] 底部是否显示"正在思考"或加载状态？
- [ ] 等待30秒后有变化吗？

### D. 场景元素
- [ ] 点击"编辑模式"能看到场景元素列表吗？
- [ ] 列表中有 `dialogue-text` 元素吗？

---

## 临时解决方案

如果急需使用，可以：

### 方案A: 使用标准对话视图
1. 点击顶部 **"对话"** 标签
2. 在标准视图中正常使用
3. 功能完全一样，只是没有 GAL 风格

### 方案B: 查看历史日志
1. 在 GAL 视图点击右上角 **"日志"** 按钮
2. 在弹出的面板中查看对话历史
3. 确认消息是否被记录

---

## 需要你提供的信息

请告诉我：

1. **F12 控制台有什么错误？**（截图或复制）
2. **发送消息后，输入框的文字是否消失？**
3. **左侧"历史"列表有新对话吗？**
4. **底部是否显示模型名称？**（如 DeepSeek-V4-Pro）
5. **点击"编辑模式"能看到什么？**

有了这些信息我可以精准定位问题！🔍

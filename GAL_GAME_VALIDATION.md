# GAL 游戏本地验证记录

日期：2026-09-07。项目路径：`F:\codex\model-router-gal-preview`。开发基础提交：`829546d`。下方试玩记录使用 `0.4.20` 开发版本；用户随后授权将本次功能发布为 `0.4.21`。

## 0.4.21 发布验收

本次发布基于上述已验收功能，更新包版本、安装文档与发布说明。npm 分发构建后的客户端、服务和剧情模块；GitHub 同时保留完整客户端源码、测试、构建脚本及生成素材，便于重建。私人测试日志、凭据、临时响应与用户参考截图不进入发布文件。

发布前确认 npm 最高稳定版本为 `0.4.20`，GitHub 主分支同为 `0.4.20`。旧 npm tarball 的全部 17 个文件与原 `v0.4.20` 标签逐字节一致。主分支后来更新过三份 README 文件，原标签和旧包仍然匹配，本次不改写它们。`0.4.21` 将从同一发布提交构建并打包，发布后校验 registry tarball、GitHub 标签源码和 Release 附件一致；全部旧版本、标签和 Release 保留。

## 本轮：群像剧情扩写

离线剧情从 200 个节点、7 处选择扩展为 **1,111 个节点、28 处选择、5 种关系结局及各自后日谈**。保留 DeepSeek 关系主线，新增 14 位人物各自带选择的个人事件、公共记忆展、暴雨与档案转移、凌晨夜谈、展览开放及回廊回收。新增三个内容文件：`gal-story-personal.mjs`、`gal-story-ensemble.mjs`、`gal-story-afterword.mjs`。

默认选择的普通完整路线有 853 次推进、882 条含玩家选项的阅读记录、22,722 个文本字符，约 2.27 万字。全图有 46 个地点与时段组合；这条路线里每位角色有 35 至 86 条对白、跨 16 至 24 个场景时段。五条后日谈分别延续恋爱、友情、继续认识、保持距离与告别，没有把后两类强制改成重逢。

存档格式保持版本 1，新篇使用 `contentRevision: 2`，允许路径长度从 384 增至 2,048。旧存档按原短篇继续，位置、选项效果和旧结局保留。新开局进入完整扩展篇；不自动往玩家已经读过的路径里插入场景或选择。引擎内部已验证的状态和嵌套路径记录均冻结，避免重复校验拖慢长篇，同时保持外部存档重放校验。

| 检查 | 本轮结果 |
| --- | --- |
| `npm run build:client` / `npm run check:client` | 使用已安装 Harness 的真实 UI 包构建，一致性检查通过；未跳过 |
| `npm test` | 133 项通过，0 失败、0 跳过，其中 15 项剧情与存储测试 |
| 全剧情图 | 1,111 个节点全部可达，无断链或循环，全部选择出口有效 |
| 关系及新增选择 | 原 7 处关系选择的 2,187 种组合均到达有效结局；21 处新增选择的全部选项均实际推进，14 处个人事件与群像事件有后续台词回收 |
| 角色持续出场 | 普通路线中每位角色均超过 20 条独立台词、跨至少 3 个场景时段，实际为 35 至 86 条、16 至 24 个时段 |
| 旧版兼容 | 修改前 200 节点构建提取的早期、中段、完成三份真实路径夹具，原节点、分数、选择、结局与后续推进均通过；自动和手动存档可重新保存 |
| `node scripts/check-gal-story-ui.mjs` | 实际阅读 853 步、28 次选择至 `afterword-romance-18`，超过旧长度上限后的存档、刷新恢复和历史检查通过；28 项桌面/手机美术框检查通过；0 模型回合、0 页面错误 |
| `node scripts/check-harness-gal.mjs --session-title 米 --story-expansion` | 实际 Harness 通过原对话、编辑器、双模式隔离与普通点击回归；逐步阅读新增开场至 ChatGPT 个人事件，确认内容修订 2；1440/390 新章节的美术框、立绘、正文安全区与截图通过，0 页面错误、剧情不调用模型 |

关系组合检查覆盖 7 处关系选择的全部组合；新增个人和群像选择按每个选项验证，不宣称穷举 `3^28` 种组合。浏览器完整阅读的是一条代表性长路线，其余结局及后日谈通过引擎图和关系测试核查。本轮没有额外请求语言模型或图片 API，也没有新增场景图片，仍使用空白与文字描述。

最终报告：`test-artifacts/gal-story-ui-report.json`。实际宿主新章节截图：`test-artifacts/harness-gal-expanded-1440.png`、`harness-gal-expanded-390.png`；后日谈截图：`gal-story-ending-desktop.png`、`gal-story-ending-mobile.png`。

本机 Harness 继续在 `http://127.0.0.1:55923/` 运行。重新构建后刷新页面；旧存档若已完成，先手动保存，再在「剧情与显示设置」里重新开始可进入新篇。普通游戏仍不显示好感、信任和剧情进度；自由模式规则和美术资源未修改。没有提交、推送、发布、修改版本号或替换日常安装。

## 历史记录：双模式与人物美术对话框

游戏继续内嵌于「GAL视窗 → GAL游戏」。默认进入无需 API 的「剧情模式」，原有真实模型自由输入保留在「自由模式」。两种模式的进度和手动存档独立，切换保留各自的阅读位置或输入草稿。普通画面不显示关系数值、路线条件或内部判定；开发调试默认关闭且不持久化。

剧情模式包含 200 个台词与事件节点、7 处三选一分支、5 种结局，14 位人物登场，关系主线围绕 DeepSeek。它不是 14 条已经完成的恋爱路线。对话回顾、文字速度、自动播放、三个手动存档、JSON 导入导出与重新开始已接入。读取剧情存档时重放选择，忽略外部注入的分数和标记。

本轮为对话框新增了图片素材：DeepSeek 使用原有美术框，其余 13 位通过用户授权的图片 API 生成独立设计，运行用 14 张 WebP 共约 2.63 MB。原始 PNG、处理记录和对照图均保留。对话框复用从原 `StageView` 提取的 `DialogueBox`；游戏正文位于美术框内，移动端保留徽章、边饰及放大的原图名牌。长台词从首行开始，补全文字不跳到底部，「后续台词」先翻阅当前正文再推进剧情。

场景仍为空白与文字描述。原有六张 DeepSeek 表情继续使用；悬浮装饰、桌宠和费用气泡保持现有行为。版本仍为 `0.4.20`，没有提交、推送、发布或替换日常安装。

### 本轮验证结果

| 检查 | 结果 |
| --- | --- |
| `npm run build:client` | 使用当前已安装 Harness 的真实 UI 包构建成功 |
| `npm run check:client` | 客户端产物与源码一致，未跳过 |
| `npm test` | 127 项通过，0 失败、0 跳过，含原有路由、GAL、模型选择和更新逻辑回归 |
| 剧情分支穷举 | 全部 2187 种选择组合均到达有效结局；14 位人物、7 处分支与5 种结局均可达；错误路径、跨模式存档和注入分数校验通过 |
| `node scripts/check-gal-ui.mjs` | 14 次可控自由互动通过；正负变化、隐藏状态、调试关闭、错误重试、存读档、六幕与结局；补充等待状态与长回复首行及翻阅检查 |
| `node scripts/check-gal-story-ui.mjs` | 14 位人物 × 1440/390 两种视口共 28 个美术框通过；138 步剧情、7 次选项至 `romance-08` 结局；存读档、刷新、默认剧情、隐藏调试和模式存档隔离通过；0 模型请求、0 页面错误 |
| `node scripts/check-harness-gal.mjs --session-title 米` | 实际宿主双模式、普通点击、存档隔离、原对话输入与编辑器、场景未改、默认剧情不请求模型通过；1440×1000、430×860、390×844 无页面错误和横向溢出 |
| 美术框图片检查 | 14 张 WebP 的透明通道、尺寸、布局元数据校验通过；全部 28 个图鉴画面的图片解码、非空像素、独立图片内容、姓名和正文安全区通过；实际 Harness 中 DeepSeek 与豆包另行验证通过 |
| `git diff --check` | 通过 |

本轮没有新增真实文字模型调用。自由模式的自动化流程使用可控模型响应；真实正负关系回合属于下方历史基线。图片 API 生成已实际执行，生成及兼容处理记录见 `output/imagegen/dialogue-frames/README.md`。

最终检查修正了 Grok、Kimi、MiniMax 的移动名牌内区偏矮问题：按原图比例放大名牌，文字区至少 18px；再次验证全部人物的 390/1440 布局。自动化在视口变化后等待两帧，使 `ResizeObserver` 完成布局再量文字边界，未放宽断言。报告见 `test-artifacts/gal-story-ui-report.json`。该尺寸修正后再次完成客户端构建、一致性检查和 127 项项目测试；实际宿主全流程已在同一套 14 资源上通过。

实际 Harness 最终画面：`test-artifacts/harness-gal-desktop.png`、`harness-gal-mobile-390.png`、`harness-gal-mobile-430.png`、`harness-gal-story-desktop.png`、`harness-gal-story-mobile.png`。全部人物对照图：`output/imagegen/dialogue-frames/runtime-contact-sheet.png`。这些截图取自测试浏览器上下文，不修改用户日常游戏进度；没有向原工作会话发送任务消息。

本机 Harness 试玩地址为 `http://127.0.0.1:55923/`，辅助独立预览为 `http://127.0.0.1:61533/`。客户端不热更新，重新构建后需要刷新已打开页面。最终验收以当前源码、构建和截图为准，以下历史记录中的旧入口和旧面板不代表当前实现。

## 历史记录：单自由模式移入 GAL 视窗

- DeepSeek 单角色六幕样章《未写完的约定》移入原「GAL视窗」的「GAL游戏」标签；原「游戏模式」改名为「对话模式」，编辑模式保留。
- 移除独立侧栏游戏入口与全屏游戏弹层。普通画面只显示场景、角色、对话、输入与轻量菜单。
- 好感、信任、隔阂、关系阶段、情绪标签、进度和判定证据在后台运行，普通回忆、历史、存档和结局也不显示这些信息。
- 设置内「开发调试」默认关闭且不持久化；测试人员手动开启后才能查看后台状态面板。
- 自由输入、真实模型关系评估与台词生成，好感和信任双向变化、隔阂与修复、友情与恋爱意愿、记忆、四类结局。
- 独立自动进度、三个手动存档、JSON 导入导出。场景保留空白与文字描述。
- 继续使用原始正常立绘与五张已有 API 生成差分：开心、害羞、低落、生气、思考；本轮不重新生成图片，不修改后台关系规则。
- 初级构想见 `GAL_GAME_CONCEPT.zh.md`，启动说明见 `GAL_GAME_PREVIEW.md`，图片提示词与生成记录见 `output/imagegen/`。

### 当时验证

本轮客户端构建与产物一致性检查通过，项目测试 118 项通过。独立预览的 14 次可控测试互动覆盖普通画面和菜单隐藏后台信息、调试开关不改变存档且刷新后关闭、存读档、错误重试及完整六幕流程。

真实 Harness 在已有会话中通过「GAL视窗 → GAL游戏」进入，确认游戏内嵌、原对话输入与编辑器可用、切换模式保留游戏存档、原会话草稿和编辑场景未修改。游戏测试使用新的浏览器上下文；没有向原工作会话发送任务消息。

桌面 1440×1000 与手机 430×860、390×844 均通过，无页面错误、无横向溢出；六表情在两种尺寸的 12 项实际加载检查通过。模式标签对比度修正后：常态 11.94:1、悬停 11.98:1、选中 6.66:1。悬浮装饰保留现有行为，未修改永久设置。

内嵌模式完成一次真实 DeepSeek-V4-Flash 回合，revision 从 0 到 1，好感与信任各增加 1，普通界面只呈现自然台词与立绘，没有显示数值。后端规则和图片资源本轮未修改，没有重新生成图片。

本轮截图：`test-artifacts/harness-gal-desktop.png`、`harness-gal-mobile-430.png`、`harness-gal-mobile-390.png`、`harness-gal-debug.png`、`harness-gal-original-dialogue.png`、`harness-gal-original-editor.png`。

## 历史记录：独立侧栏入口初版

以下结果来自本轮修改前的独立侧栏入口和游戏弹层版本，保留为历史基线。测试数量、入口截图与端到端流程不能直接用作本轮改动的验收结论。

| 检查 | 结果 |
| --- | --- |
| `npm run build:client` | 使用已安装 Harness 的真实 UI 包构建成功，未跳过 |
| `npm run check:client` | 客户端产物与源码一致 |
| `npm test` | 118 项通过，0 失败，0 跳过，包含原有路由、GAL、模型选择与更新逻辑回归 |
| 图片 API 适配器离线测试 | 21 项通过，没有发起额外图片请求 |
| 独立预览 Playwright | 1440×960、390×844；自由输入、加减分、回忆、存读档、刷新恢复、失败重试、原 GAL 存储隔离、六幕及结局通过 |
| 前版真实 Harness Playwright | 当时的侧栏入口支持无任务进入游戏，模型目录可读取，关闭可返回工作区，再次进入可恢复；1440×1000 与 430×860 无页面错误、无横向溢出。该入口本轮已移除 |
| 表情实际加载 | 6 张图片分别在 1440×1000 和 390×844 验证；图片解码、非空像素、不同图像内容、生成 WebP 格式与区域不重叠均通过 |
| 真实模型正向回合 | DeepSeek-V4-Flash，经宿主 LLM 服务完成评估和回应；revision 1，好感 +1、信任 +1 |
| 真实模型负向回合 | revision 2，好感 -2、信任 -3、隔阂 1，情绪低落、立绘 sad，角色生成相应回应 |
| `git diff --check` | 通过 |

前版真实正向输入为愿意陪她一起写信，并询问她希望带去新城的小事；负向输入为直接贬低并要求她闭嘴。测试使用独立浏览器上下文，未写入用户的日常游戏进度。端到端正负回合是实际模型输出；六幕流程及存档异常测试使用可控模型测试数据。

## 宿主环境与历史兼容性记录

测试使用当前机器已安装的 Harness CLI、真实 Web 客户端和独立 `DSH_HOME`：`test-artifacts/dsh-host-home`。本地实例监听 `http://127.0.0.1:55923`，临时认证入口保存在忽略目录的 `dsh-host2.log` 中。日常 desktop profile 未被替换。

原有增强包和 Ego Browser 保留。新模式通过独立 RPC 通道调用宿主模型，不发送工作会话消息、不授予工具、不改变原会话选择的模型。所选原 DeepSeek 凭据只传入测试子进程内存，未复制到交付文件。

前版首次真实验证曾遇到台词没有返回合法 JSON；随后将台词协议调整为自然中文，表情取自已审核情绪。关系评估仍严格解析与校验 JSON，畸形结构化回复仍会失败回滚。修正后的前版真实正负回合完整通过。

## 当前试玩边界

剧情模式已有 14 位人物个人事件、群像长篇及五条后日谈，关系主线仍围绕 DeepSeek；自由模式仍是 DeepSeek 六幕初版。其他人物独立恋爱路线和完整约会系统属于后续规划。模型判断仍可能偏差；复杂语境、含蓄分手与讽刺表达需要继续试玩，不能将当前规则视为完全可靠的自然语言理解。

自由模式的亲密节奏受每幕加分上限影响，正常推进最早第五幕才可能达到恋爱门槛。存档在本地浏览器，未提供跨设备同步。自由模式每个成功回合通常使用两次模型调用；剧情模式不调用模型。

表情经过图片 API 编辑，人物整体一致但存在细微重绘；思考图顶部有新增细发丝。原始图片、适配后的 PNG、运行用 WebP 和六表情对照图均保留。实际输出尺寸与处理方式记录在图片 README。

前版截图位于忽略目录 `test-artifacts/`：`harness-initial.png`、`harness-gal-desktop.png`、`harness-gal-mobile.png`、`harness-gal-live.png`、`gal-ending.png`。截图中的入口与状态显示可能已过时，不能作为本轮最终画面。没有把真实模型测试覆盖描述为全剧情或全功能的完整人工验收。

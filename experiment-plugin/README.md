# Model Router Experiment Plugin

离线可复现实验套件，严格对应论文第五章的六组实验：单任务性能、权重敏感性、协作模式、在线学习、预算约束和消融分析。

```bash
npm start
```

运行后生成 `outputs/charts/*.png`、`outputs/charts/*.md`、
`outputs/data/raw-results.json` 和 `outputs/reports/EXPERIMENT_REPORT.md`。

